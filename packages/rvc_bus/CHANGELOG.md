## 0.1.3

- Add README, CHANGELOG, LICENSE, and example for pub.dev.
- Add repository, homepage, and topic metadata to pubspec.
- Add unit tests: RvcName encoding, device info, sealed event hierarchy.

## 0.1.0

- Initial release.
- `RvcBusRegistry` for automatic RV-C device discovery.
- Online/offline tracking with configurable timeouts.
- `RvcName` with 64-bit NAME decoding and RV-C device function lookup.
- Sealed `RvcBusEvent` hierarchy with four event types.
