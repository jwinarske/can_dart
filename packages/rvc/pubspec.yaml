name: rvc
description: >-
  RV-C protocol layer on package:j1939. DGN definitions, command/status
  helpers, and RvcEcu with RV-C NAME defaults for recreation vehicles.
version: 0.1.3
repository: https://github.com/jwinarske/can_dart/tree/main/packages/rvc
homepage: https://github.com/jwinarske/can_dart
issue_tracker: https://github.com/jwinarske/can_dart/issues
topics:
  - can-bus
  - rvc
  - rv

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  j1939: ^0.1.0
  can_codec: ^0.1.0

executables:
  rvc_demo:
  rv_sim:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
