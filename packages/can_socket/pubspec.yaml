name: can_socket
description: Pure Dart FFI bindings for Linux SocketCAN. Raw CAN, CAN FD, BCM, ISO-TP, and J1939.
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  ffi: ^2.1.4

dev_dependencies:
  test: ^1.25.8
  lints: ^5.1.1
