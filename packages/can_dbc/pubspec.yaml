name: can_dbc
description: Pure Dart DBC file parser and signal compiler for CAN bus databases.
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  ffi: ^2.1.4

dev_dependencies:
  test: ^1.25.8
  lints: ^5.1.1
