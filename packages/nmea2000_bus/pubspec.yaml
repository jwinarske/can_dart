name: nmea2000_bus
description: >-
  NMEA 2000 bus topology tracker and device registry. Auto-discovers
  devices, tracks online/offline state, and exposes reactive bus events.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  nmea2000:
    path: ../nmea2000

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
